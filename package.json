{
  "name": "lens-protocol",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ anudit/lens-protocol",
    "create-local": "graph create --node http://localhost:8020/ anudit/lens-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ anudit/lens-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 anudit/lens-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
