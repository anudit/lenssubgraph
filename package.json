{
  "name": "lensterdam",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ franz101/lens-protocol",
    "create-local": "graph create --node http://localhost:8020/ franz101/lens-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ franz101/lens-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 franz101/lens-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "~0.26.0",
    "@graphprotocol/graph-ts": "~0.24.1"
  }
}
